{
  "name": "naked-usdr",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ st4rgarden/naked-usdr",
    "create-local": "graph create --node http://localhost:8020/ st4rgarden/naked-usdr",
    "remove-local": "graph remove --node http://localhost:8020/ st4rgarden/naked-usdr",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 st4rgarden/naked-usdr"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
